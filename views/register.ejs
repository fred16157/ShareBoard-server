<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <script src="/js/bootstrap.min.js"></script>
    <title>회원가입</title>
</head>

<body style="width: 100%; height: 100%;">
    <div class="container h-100">
        <div class="row justify-content-center d-flex align-items-center">
            <% if(!success) { %>
            <form action="/register" method="POST" class="col-8" >
                <div class="card border-info mb-3">
                    <div class="card-body">
                        <h4 class="card-title">회원가입</h4>
                        <% if(failed == 'exists') { %>
                            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                                <strong>회원가입 실패</strong> 이미 존재하는 유저 이름입니다.
                                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                        <% } %>
                        <div class="mb-3">
                            <label for="username" class="form-label">사용자 이름</label>
                            <input type="text" class="form-control" id="username" name="username" autocomplete="off" placeholder="사용자 이름">
                        </div>
                        <div class="mb-3">
                            <label for="password" class="form-label">비밀번호</label>
                            <input type="password" class="form-control" id="password" name="password" autocomplete="off" placeholder="비밀번호">
                        </div>
                        <div class="mb-3">
                            <label for="passcheck" class="form-label">비밀번호 확인</label>
                            <input type="password" class="form-control" id="passcheck" autocomplete="off" placeholder="비밀번호 확인">
                        </div>
                        <div class="text-end">
                            <button type="submit" class="btn btn-primary">가입</button>
                        </div>
                    </div>
                </div>
            </form>
            <% } else { %>
                <div class="card border-success mb-3 col-8">
                    <div class="card-body">
                        <h4 class="card-title">회원가입 완료</h4>
                        <p>회원가입이 정상적으로 완료되었습니다.</p>
                    </div>
                </div>
            <% } %>
        </div>
    </div>
</body>

</html>